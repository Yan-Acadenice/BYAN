// Global test setup for renderer tests.
// Extends Vitest's expect (available via globals: true in vitest.renderer.config.ts)
// with @testing-library/jest-dom matchers.
import '@testing-library/jest-dom';
